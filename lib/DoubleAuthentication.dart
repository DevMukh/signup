import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class DoubleAthen extends StatelessWidget {
  const DoubleAthen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Check Authentication'),
      ),
    );
  }
}
